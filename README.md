Writer Style
============

This repository contains the code and data for the **writer-style** project.

## Setup

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

## Overview

- Data under `data/` and `dataset/`
- Training scripts under `scripts/`
- Models and checkpoints under `models/`
